{% extends 'FdaPlayerBundle::layout.html.twig' %}

{% block content -%}
    <h1>Tournament list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Gamemode</th>
                <th>Gamecount</th>
                <th>Legmode</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tournament_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.gameMode }}</td>
                <td>{{ entity.gameCount }}</td>
                <td>{{ entity.legMode }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tournament_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tournament_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tournament_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
