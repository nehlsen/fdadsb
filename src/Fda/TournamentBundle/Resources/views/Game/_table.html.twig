<table class="table">
    <tbody>
    {% for game in games %}
        <tr>
            <td>
                <a href="{{ path('game_show',{gameId:game.id}) }}">
                    <i class="fa fa-eye"></i>
                </a>
            </td>
            <td>
                {{ player_link(game.player1) }}
                {% if game.winner == game.player1 %}
                    <i class="fa fa-trophy"></i>
                {% endif %}
            </td>
            <td>{{ game.legsWonPlayer1 }}</td>
            <td>{{ game.legsWonPlayer2 }}</td>
            <td>
                {{ player_link(game.player2) }}
                {% if game.winner == game.player2 %}
                    <i class="fa fa-trophy"></i>
                {% endif %}
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>