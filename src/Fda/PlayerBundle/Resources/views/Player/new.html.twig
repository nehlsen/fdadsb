{% extends 'FdaPlayerBundle::layout.html.twig' %}

{% block content -%}
    <h1>Player creation</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('player') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
